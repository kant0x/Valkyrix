---
phase: 02-menu-network-architecture
plan: 05
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/screens/EscMenuOverlay.ts
  - src/screens/HudOverlay.ts
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "ESC key toggles the ESC overlay while the game continues running"
    - "ESC overlay shows music toggle and Exit to Menu button"
    - "Exit to Menu button shows a confirmation warning before navigating"
    - "Game loop (requestAnimationFrame) is NOT paused when ESC overlay is visible"
    - "HUD is always visible at the bottom of the game screen"
    - "HUD can be updated with wave, health, resource values"
  artifacts:
    - path: "src/screens/EscMenuOverlay.ts"
      provides: "ESC key overlay (no pause) with music settings and exit confirmation"
      exports: ["EscMenuOverlay"]
    - path: "src/screens/HudOverlay.ts"
      provides: "Persistent in-game HUD at bottom of screen"
      exports: ["HudOverlay"]
  key_links:
    - from: "src/screens/EscMenuOverlay.ts"
      to: "document"
      via: "keydown event listener for Escape key"
      pattern: "addEventListener.*keydown"
    - from: "src/screens/EscMenuOverlay.ts"
      to: "src/screens/ScreenManager.ts"
      via: "manager.navigateTo('menu') on exit confirmation"
      pattern: "navigateTo.*menu"
---

<objective>
Implement EscMenuOverlay (ESC key overlay that does not pause the game) and HudOverlay (persistent in-game HUD). These are parallel to Plans 03 and 04 — no shared files.

Purpose: The locked design specifies the ESC overlay must not stop the game loop (real-time). The HUD is always visible during gameplay. Both are DOM overlays on top of the canvas.
Output: `src/screens/EscMenuOverlay.ts`, `src/screens/HudOverlay.ts`.
</objective>

<execution_context>
@E:/py_scrypt/карта валкирикс/.claude/get-shit-done/workflows/execute-plan.md
@E:/py_scrypt/карта валкирикс/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-menu-network-architecture/02-CONTEXT.md
@.planning/phases/02-menu-network-architecture/02-RESEARCH.md

<interfaces>
<!-- Contracts from Plan 02 -->

From src/screens/ScreenManager.ts:
```typescript
export type Screen = 'wallet' | 'menu' | 'game';
export interface ScreenModule {
  mount(container: HTMLElement): void;
  unmount(): void;
}
export class ScreenManager {
  navigateTo(screen: Screen): void;
  currentScreen(): Screen | null;
}
```

Existing game loop in src/main.ts (must NOT be touched by these modules):
```typescript
requestAnimationFrame(frame);  // game loop — runs independently, never paused
```

Color palette:
```
background: #07111d
overlay: rgba(7, 17, 29, 0.85)
accent: rgba(151, 194, 235, 0.18)
text: #c8d8e8
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement EscMenuOverlay</name>
  <files>src/screens/EscMenuOverlay.ts</files>
  <action>
Create `src/screens/EscMenuOverlay.ts`. This is NOT a ScreenModule — it is a separate overlay that lives on top of the game screen. It is mounted when the game screen is mounted and unmounted when the game screen is unmounted.

CRITICAL: The ESC overlay must NOT touch requestAnimationFrame or the canvas. It only shows/hides a DOM div. The game loop runs continuously regardless.

Design (Claude's discretion):
- Fixed-position overlay, z-index 100, background rgba(7,17,29,0.85)
- Contents: music toggle checkbox, "Exit to Menu" button
- "Exit to Menu" shows an inline confirmation: "Progress will be lost. Exit?" with Confirm and Cancel buttons
- ESC key toggles visibility; clicking outside the overlay panel does NOT close it (game is real-time, accidental close = bad)

```typescript
// src/screens/EscMenuOverlay.ts
import type { ScreenManager } from './ScreenManager';

const STYLE_ID = 'vk-esc-overlay-style';

export class EscMenuOverlay {
  private el: HTMLElement | null = null;
  private visible = false;
  private musicEnabled = true;
  private readonly manager: ScreenManager;

  constructor(manager: ScreenManager) {
    this.manager = manager;
  }

  mount(container: HTMLElement): void {
    this.ensureStyle();

    const el = document.createElement('div');
    el.id = 'vk-esc-overlay';
    el.style.display = 'none';
    el.innerHTML = `
      <div class="vk-esc-panel">
        <h2 class="vk-esc-title">Menu</h2>
        <label class="vk-esc-music">
          <input type="checkbox" id="vk-music-toggle" checked />
          Music
        </label>
        <button id="btn-exit-menu" class="vk-esc-btn">Exit to Menu</button>
        <div id="vk-exit-confirm" class="vk-exit-confirm" style="display:none">
          <p>Progress will be lost. Exit?</p>
          <div class="vk-confirm-buttons">
            <button id="btn-confirm-exit" class="vk-esc-btn vk-btn-danger">Exit</button>
            <button id="btn-cancel-exit" class="vk-esc-btn">Stay</button>
          </div>
        </div>
        <p class="vk-esc-hint">Press ESC to close</p>
      </div>
    `;
    container.appendChild(el);
    this.el = el;

    this.bindEvents(el);
    document.addEventListener('keydown', this.onKey);
  }

  unmount(): void {
    document.removeEventListener('keydown', this.onKey);
    this.el?.remove();
    this.el = null;
    this.visible = false;
  }

  toggle(): void {
    this.visible = !this.visible;
    if (this.el) {
      this.el.style.display = this.visible ? 'flex' : 'none';
    }
  }

  isMusicEnabled(): boolean {
    return this.musicEnabled;
  }

  private onKey = (e: KeyboardEvent): void => {
    if (e.key === 'Escape') {
      e.preventDefault();
      this.toggle();
    }
  };

  private bindEvents(el: HTMLElement): void {
    const musicToggle = el.querySelector<HTMLInputElement>('#vk-music-toggle');
    const exitBtn = el.querySelector<HTMLButtonElement>('#btn-exit-menu');
    const confirmEl = el.querySelector<HTMLElement>('#vk-exit-confirm');
    const confirmExitBtn = el.querySelector<HTMLButtonElement>('#btn-confirm-exit');
    const cancelExitBtn = el.querySelector<HTMLButtonElement>('#btn-cancel-exit');

    musicToggle?.addEventListener('change', () => {
      this.musicEnabled = musicToggle.checked;
    });

    exitBtn?.addEventListener('click', () => {
      if (confirmEl) confirmEl.style.display = 'block';
      if (exitBtn) exitBtn.style.display = 'none';
    });

    confirmExitBtn?.addEventListener('click', () => {
      this.unmount();
      this.manager.navigateTo('menu');
    });

    cancelExitBtn?.addEventListener('click', () => {
      if (confirmEl) confirmEl.style.display = 'none';
      if (exitBtn) exitBtn.style.display = 'block';
    });
  }

  private ensureStyle(): void {
    if (document.getElementById(STYLE_ID)) return;
    const style = document.createElement('style');
    style.id = STYLE_ID;
    style.textContent = `
      #vk-esc-overlay {
        position: fixed; inset: 0;
        background: rgba(7,17,29,0.85);
        align-items: center; justify-content: center;
        z-index: 100;
      }
      .vk-esc-panel {
        background: #0b1220; border: 1px solid rgba(151,194,235,0.25);
        border-radius: 8px; padding: 32px 40px;
        display: flex; flex-direction: column; gap: 20px; align-items: center;
        min-width: 280px;
      }
      .vk-esc-title { color: #c8d8e8; font-family: sans-serif; font-size: 1.4rem; }
      .vk-esc-music { color: #8aa8c8; font-family: sans-serif; display: flex; gap: 8px; align-items: center; cursor: pointer; }
      .vk-esc-btn {
        background: rgba(151,194,235,0.18); color: #c8d8e8;
        border: 1px solid rgba(151,194,235,0.35); border-radius: 6px;
        padding: 10px 28px; font-size: 0.95rem; cursor: pointer;
        font-family: sans-serif; width: 100%;
        transition: background 0.15s;
      }
      .vk-esc-btn:hover { background: rgba(151,194,235,0.28); }
      .vk-btn-danger { border-color: rgba(220,80,80,0.5); color: #e07070; }
      .vk-btn-danger:hover { background: rgba(220,80,80,0.2); }
      .vk-exit-confirm { display: flex; flex-direction: column; gap: 12px; align-items: center; width: 100%; }
      .vk-exit-confirm p { color: #c8d8e8; font-family: sans-serif; font-size: 0.9rem; }
      .vk-confirm-buttons { display: flex; gap: 10px; width: 100%; }
      .vk-esc-hint { color: #4a6a8a; font-size: 0.75rem; font-family: sans-serif; }
    `;
    document.head.appendChild(style);
  }
}
```
  </action>
  <verify>
    <automated>cd "E:/py_scrypt/карта валкирикс" && npx tsc --noEmit 2>&1 | grep -i "escmenuoverlay\|EscMenuOverlay" | head -10 || echo "no type errors for EscMenuOverlay"</automated>
  </verify>
  <done>EscMenuOverlay.ts compiles without TypeScript errors. File exports EscMenuOverlay class with mount(), unmount(), toggle(), isMusicEnabled() methods. Does not import or reference requestAnimationFrame or canvas.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HudOverlay</name>
  <files>src/screens/HudOverlay.ts</files>
  <action>
Create `src/screens/HudOverlay.ts`. The HUD is a fixed-position DOM bar at the bottom of the screen, always visible during gameplay.

The HUD for Phase 2 is a structural placeholder — it shows the slots for wave number, health, and resources, but with stub "—" values. Phase 3 (units/combat) fills in real data.

Design: Single horizontal bar at bottom, dark background, three info blocks (Wave, Health, Resources). Values set via update() method.

```typescript
// src/screens/HudOverlay.ts
// Persistent in-game HUD bar. Mounted by the game screen, not ScreenManager.
// Phase 2: structural placeholder with stub values.
// Phase 3: call update({ wave, health, resources }) with real game data.

const STYLE_ID = 'vk-hud-style';

export interface HudState {
  wave?: number | string;
  health?: number | string;
  resources?: number | string;
}

export class HudOverlay {
  private el: HTMLElement | null = null;

  mount(container: HTMLElement): void {
    this.ensureStyle();

    const el = document.createElement('div');
    el.id = 'vk-hud';
    el.innerHTML = `
      <div class="vk-hud-block">
        <span class="vk-hud-label">Wave</span>
        <span id="vk-hud-wave" class="vk-hud-value">—</span>
      </div>
      <div class="vk-hud-block">
        <span class="vk-hud-label">Citadel HP</span>
        <span id="vk-hud-health" class="vk-hud-value">—</span>
      </div>
      <div class="vk-hud-block">
        <span class="vk-hud-label">Resources</span>
        <span id="vk-hud-resources" class="vk-hud-value">—</span>
      </div>
    `;
    container.appendChild(el);
    this.el = el;
  }

  unmount(): void {
    this.el?.remove();
    this.el = null;
  }

  update(state: HudState): void {
    if (!this.el) return;
    if (state.wave !== undefined) {
      const el = this.el.querySelector('#vk-hud-wave');
      if (el) el.textContent = String(state.wave);
    }
    if (state.health !== undefined) {
      const el = this.el.querySelector('#vk-hud-health');
      if (el) el.textContent = String(state.health);
    }
    if (state.resources !== undefined) {
      const el = this.el.querySelector('#vk-hud-resources');
      if (el) el.textContent = String(state.resources);
    }
  }

  private ensureStyle(): void {
    if (document.getElementById(STYLE_ID)) return;
    const style = document.createElement('style');
    style.id = STYLE_ID;
    style.textContent = `
      #vk-hud {
        position: fixed; bottom: 0; left: 0; right: 0;
        height: 48px;
        background: rgba(7,17,29,0.9);
        border-top: 1px solid rgba(151,194,235,0.15);
        display: flex; align-items: center; justify-content: center;
        gap: 48px; z-index: 50;
      }
      .vk-hud-block { display: flex; align-items: center; gap: 8px; }
      .vk-hud-label { color: #4a6a8a; font-family: sans-serif; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; }
      .vk-hud-value { color: #c8d8e8; font-family: sans-serif; font-size: 0.95rem; min-width: 2ch; }
    `;
    document.head.appendChild(style);
  }
}
```
  </action>
  <verify>
    <automated>cd "E:/py_scrypt/карта валкирикс" && npx tsc --noEmit 2>&1 | grep -i "hudoverlay\|HudOverlay" | head -10 || echo "no type errors for HudOverlay"</automated>
  </verify>
  <done>HudOverlay.ts compiles without TypeScript errors. Exports HudOverlay class with mount(), unmount(), update(HudState) methods. Exports HudState interface. HUD renders at bottom of screen with Wave, Citadel HP, Resources labels.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no TypeScript errors across all screens/ files
- EscMenuOverlay.ts: does not reference canvas or requestAnimationFrame
- EscMenuOverlay toggle() responds to Escape key via document keydown listener
- EscMenuOverlay unmount() removes the keydown listener (no leak)
- HudOverlay update() method exists with HudState parameter
</verification>

<success_criteria>
EscMenuOverlay provides ESC-key toggle overlay with music setting and exit-with-confirmation flow, without touching the game loop. HudOverlay provides a persistent bottom bar with update() API for Phase 3 to fill in real game data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-menu-network-architecture/02-05-SUMMARY.md`
</output>
