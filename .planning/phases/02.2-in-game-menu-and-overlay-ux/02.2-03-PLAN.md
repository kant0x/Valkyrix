---
phase: 02.2-in-game-menu-and-overlay-ux
plan: 03
type: execute
wave: 3
depends_on: [02.2-01-PLAN.md, 02.2-02-PLAN.md]
files_modified:
  - src/screens/EscMenuOverlay.ts
  - src/screens/EscMenuOverlay.test.ts
autonomous: false
requirements: [UI-02, GAME-01, GAME-02]

must_haves:
  truths:
    - "ESC remains a compact system overlay and does not become the main war-control surface"
    - "ESC overlay provides explicit Resume, Music, and Exit to Menu actions while match simulation remains unpaused"
    - "Exit confirmation copy matches the destructive contract from 02.2-UI-SPEC.md"
    - "In-match command layer, ESC overlay, and result overlay feel like one visual family during human verification"
  artifacts:
    - path: "src/screens/EscMenuOverlay.ts"
      provides: "Compact system overlay aligned to the new command-layer style"
      exports: ["EscMenuOverlay"]
    - path: "src/screens/EscMenuOverlay.test.ts"
      provides: "Behavioral tests for ESC toggle, resume, and exit confirmation flow"
  key_links:
    - from: "src/screens/EscMenuOverlay.ts"
      to: "src/screens/ScreenManager.ts"
      via: "confirmed exit still routes to the main menu"
      pattern: "navigateTo\\('menu'\\)"
---

<objective>
Polish the system overlay side of the in-match UX and perform the first full human verification pass for the new command layer.

Purpose: After this plan, the player sees one coherent in-match UX family: persistent battle console below, compact system overlay on ESC, and aligned end-of-match presentation.
Output: Updated `EscMenuOverlay.ts`, tests, and a blocking human verification checkpoint.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-CONTEXT.md
@.planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-UI-SPEC.md
@.planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-01-PLAN.md
@.planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-02-PLAN.md
@src/screens/EscMenuOverlay.ts
@src/screens/HudOverlay.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh EscMenuOverlay as a compact system menu</name>
  <files>src/screens/EscMenuOverlay.ts</files>
  <read_first>
    - src/screens/EscMenuOverlay.ts
    - src/screens/HudOverlay.ts
    - .planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-CONTEXT.md
    - .planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-UI-SPEC.md
  </read_first>
  <action>
    Keep the no-pause ESC behavior, but restyle and tighten the overlay into a compact system menu.

    Apply these exact copy decisions:
    - Title: `System`
    - Resume button: `Return to Battle`
    - Exit button: `Exit to Menu`
    - Exit confirmation body: `Progress from this battle will be lost. Leave the field?`

    The overlay must keep music/settings and exit flow, but it must not contain tower or unit battle actions.

    Add an explicit Resume button that closes the overlay without requiring a second ESC press.

    Keep exit confirmation inline inside the overlay panel. Keep `manager.navigateTo('menu')` only on confirmed exit.

    Maintain local style injection in `ensureStyle()` and align its tokens to the same dark dominant / raised secondary / blue accent / destructive red family already declared in `02.2-UI-SPEC.md`.
  </action>
  <acceptance_criteria>
    - `src/screens/EscMenuOverlay.ts` contains `System`
    - `src/screens/EscMenuOverlay.ts` contains `Return to Battle`
    - `src/screens/EscMenuOverlay.ts` contains `Exit to Menu`
    - `src/screens/EscMenuOverlay.ts` contains `Progress from this battle will be lost. Leave the field?`
    - `src/screens/EscMenuOverlay.ts` contains `navigateTo('menu')`
  </acceptance_criteria>
  <verify>
    <automated>cd "E:/py_scrypt/карта валкирикс" && npx tsc --noEmit</automated>
  </verify>
  <done>ESC overlay is clearly a system menu and still compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused ESC overlay behavior tests</name>
  <files>src/screens/EscMenuOverlay.test.ts</files>
  <read_first>
    - src/screens/EscMenuOverlay.ts
    - src/screens/ScreenManager.test.ts
    - src/screens/HudOverlay.test.ts
  </read_first>
  <action>
    Create `src/screens/EscMenuOverlay.test.ts` with jsdom tests for the compact system overlay.

    Add tests that prove:
    - pressing `Escape` toggles `#vk-esc-overlay`
    - clicking `Return to Battle` hides the overlay
    - clicking `Exit to Menu` reveals the exact destructive confirmation copy
    - confirming exit calls `navigateTo('menu')`
    - no build-action labels such as `Attack Tower` or `Buff Tower` appear inside the ESC overlay markup
  </action>
  <acceptance_criteria>
    - `src/screens/EscMenuOverlay.test.ts` contains `Return to Battle`
    - `src/screens/EscMenuOverlay.test.ts` contains `Progress from this battle will be lost. Leave the field?`
    - `src/screens/EscMenuOverlay.test.ts` contains `navigateTo('menu')`
    - `src/screens/EscMenuOverlay.test.ts` contains `Attack Tower`
    - `npx vitest run src/screens/EscMenuOverlay.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "E:/py_scrypt/карта валкирикс" && npx vitest run src/screens/EscMenuOverlay.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>ESC overlay behavior is covered by focused regression tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A persistent bottom command bar now replaces the old floating tower buttons, and ESC opens a compact system overlay that stays secondary to the battle controls.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`
    2. Enter a live match and confirm the old floating build buttons are gone
    3. Confirm the bottom command bar is always visible
    4. Click `Arm Attack Tower` and verify the UI shows an armed state
    5. Place a tower on a valid zone tile and confirm the command message updates in the HUD
    6. Spend resources until one tower action becomes unaffordable and verify the command bar reflects that disabled state
    7. Press `Escape` and confirm a compact `System` overlay appears without pausing the match
    8. Click `Return to Battle` and confirm the overlay closes
    9. Re-open ESC, choose `Exit to Menu`, and confirm the destructive confirmation copy appears before navigation
    10. Trigger victory or defeat and confirm the result overlay feels visually aligned with the command bar rather than looking like an unrelated legacy screen
  </how-to-verify>
  <resume-signal>Type "approved" if the full in-match command layer feels coherent, or describe the mismatch you found</resume-signal>
</task>

</tasks>

<verification>
After Tasks 1 and 2:
- `npx vitest run src/screens/EscMenuOverlay.test.ts` exits 0
- `npx tsc --noEmit` exits 0

Before completion:
- Human verification checkpoint is approved
</verification>

<success_criteria>
1. ESC remains a short system menu and does not compete with war controls
2. Bottom command bar stays the primary battle interface
3. Exit flow uses explicit destructive confirmation
4. In-match UI family feels coherent during manual verification
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-03-SUMMARY.md`
</output>
