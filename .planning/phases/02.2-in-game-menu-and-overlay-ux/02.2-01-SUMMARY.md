---
phase: 02.2-in-game-menu-and-overlay-ux
plan: 01
status: complete
updated: 2026-03-18
---

# 02.2-01 Summary

## What Changed

- Rebuilt `src/screens/HudOverlay.ts` into a persistent bottom command bar with explicit build controls, staged unit slots, live battle state, command messaging, and aligned result overlays.
- Added gameplay-facing APIs: `setCommandCallbacks`, `setBuildSelection`, `setActionAvailability`, and `setCommandMessage`.
- Extended `HudState` with armed action, message, and affordability fields.
- Added `src/screens/HudOverlay.test.ts` to lock the command-bar DOM contract and core state updates.

## Verification

- `npx vitest run src/screens/HudOverlay.test.ts src/screens/EscMenuOverlay.test.ts src/wallet/WalletService.test.ts src/screens/MainMenuScreen.test.ts src/screens/WalletSplashScreen.test.ts`
- `npx tsc --noEmit`

## Notes

- Unit command slots are intentionally staged and visually present without pretending full gameplay support yet.
- Result overlay styling now matches the same dark/blue command-layer family as the new HUD.
