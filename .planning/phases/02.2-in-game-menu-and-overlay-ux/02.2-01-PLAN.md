---
phase: 02.2-in-game-menu-and-overlay-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/HudOverlay.ts
  - src/screens/HudOverlay.test.ts
autonomous: true
requirements: [GAME-02, BLDG-01]

must_haves:
  truths:
    - "HudOverlay becomes a persistent bottom command bar with grouped regions for build actions, staged unit slots, resources, and battle status"
    - "HudOverlay still renders live wave, citadel HP, and resources from update() without requiring page reload"
    - "HudOverlay exposes explicit command-surface APIs so main.ts does not manipulate HUD DOM via ad-hoc selectors"
    - "Victory and defeat overlays use the same visual family as the command layer"
  artifacts:
    - path: "src/screens/HudOverlay.ts"
      provides: "Bottom command-bar HUD with command callbacks, selected-build state, command messages, and aligned result overlay"
      exports: ["HudOverlay", "HudState"]
    - path: "src/screens/HudOverlay.test.ts"
      provides: "DOM-level tests for command bar structure and state updates"
  key_links:
    - from: "src/screens/HudOverlay.ts"
      to: "src/main.ts"
      via: "main.ts calls hud.setCommandCallbacks(), hud.setBuildSelection(), hud.setActionAvailability(), and hud.setCommandMessage()"
      pattern: "setCommandCallbacks|setBuildSelection|setActionAvailability|setCommandMessage"
---

<objective>
Redesign the passive HUD into the real in-match command bar that Phase 02.2 is centered on.

Purpose: After this plan, the game has a stable bottom battle console ready to drive real tower controls and staged unit slots instead of relying on floating emergency buttons.
Output: Updated `HudOverlay.ts` and focused tests that lock the command-bar contract before wiring it into gameplay.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-CONTEXT.md
@.planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-UI-SPEC.md
@src/screens/HudOverlay.ts
@src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor HudOverlay into a bottom command bar with explicit gameplay-facing APIs</name>
  <files>src/screens/HudOverlay.ts</files>
  <read_first>
    - src/screens/HudOverlay.ts
    - src/main.ts
    - .planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-CONTEXT.md
    - .planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-UI-SPEC.md
  </read_first>
  <action>
    Replace the current minimal HUD strip with a structured bottom command bar mounted by `HudOverlay.mount(container)`.

    Keep the existing `update(state: HudState)` entry point, but extend `HudState` to support the in-match command layer with these exact optional fields:
    - `armedAction?: 'attack' | 'buff' | null`
    - `message?: string`
    - `canAffordAttack?: boolean`
    - `canAffordBuff?: boolean`

    Add these exact public methods to `HudOverlay`:
    - `setCommandCallbacks(callbacks: { attack?: () => void; buff?: () => void; stagedUnitA?: () => void; stagedUnitB?: () => void }): void`
    - `setBuildSelection(selection: 'attack' | 'buff' | null): void`
    - `setActionAvailability(state: { attack: boolean; buff: boolean }): void`
    - `setCommandMessage(message: string): void`

    The mounted DOM must include these exact elements:
    - `#vk-hud-build-attack`
    - `#vk-hud-build-buff`
    - `#vk-hud-unit-a`
    - `#vk-hud-unit-b`
    - `#vk-hud-wave`
    - `#vk-hud-health`
    - `#vk-hud-resources`
    - `#vk-hud-command-message`

    The two staged unit buttons must remain visibly present but disabled/locked in the first version. Their labels should communicate staged future controls rather than pretending live functionality already exists.

    Preserve the win/loss overlay ownership inside `HudOverlay.showWinLossOverlay(result)`, but restyle it to match the command-layer family from `02.2-UI-SPEC.md`: dark dominant surface, secondary raised panel, blue accent for neutral focus, destructive red only for defeat/exit emphasis.

    Keep all styling local through `ensureStyle()` in the same module. Do not introduce global CSS files.
  </action>
  <acceptance_criteria>
    - `src/screens/HudOverlay.ts` contains `armedAction?: 'attack' | 'buff' | null`
    - `src/screens/HudOverlay.ts` contains `setCommandCallbacks(`
    - `src/screens/HudOverlay.ts` contains `setBuildSelection(`
    - `src/screens/HudOverlay.ts` contains `setActionAvailability(`
    - `src/screens/HudOverlay.ts` contains `setCommandMessage(`
    - `src/screens/HudOverlay.ts` contains `id="vk-hud-build-attack"`
    - `src/screens/HudOverlay.ts` contains `id="vk-hud-build-buff"`
    - `src/screens/HudOverlay.ts` contains `id="vk-hud-unit-a"`
    - `src/screens/HudOverlay.ts` contains `id="vk-hud-unit-b"`
    - `src/screens/HudOverlay.ts` contains `id="vk-hud-command-message"`
  </acceptance_criteria>
  <verify>
    <automated>cd "E:/py_scrypt/карта валкирикс" && npx tsc --noEmit</automated>
  </verify>
  <done>HudOverlay exposes the new command-bar API and still compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the command-bar contract with focused overlay tests</name>
  <files>src/screens/HudOverlay.test.ts</files>
  <read_first>
    - src/screens/HudOverlay.ts
    - src/screens/WalletSplashScreen.test.ts
    - src/screens/MainMenuScreen.test.ts
    - .planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-UI-SPEC.md
  </read_first>
  <action>
    Create `src/screens/HudOverlay.test.ts` using the project's existing Vitest + jsdom style.

    Add tests that verify these concrete behaviors:
    - `mount(container)` renders the four command elements `#vk-hud-build-attack`, `#vk-hud-build-buff`, `#vk-hud-unit-a`, `#vk-hud-unit-b`
    - `setBuildSelection('attack')` and `setBuildSelection('buff')` produce a visible selected state marker through a deterministic class or `data-selected` attribute
    - `setActionAvailability({ attack: false, buff: true })` disables the attack action but leaves buff enabled
    - `update({ wave: 2, health: 450, citadelMaxHp: 500, resources: 120 })` writes battle values into DOM
    - `setCommandMessage('Action unavailable - not enough resources or invalid placement.')` writes the exact message into `#vk-hud-command-message`

    Prefer deterministic DOM assertions over style snapshots.
  </action>
  <acceptance_criteria>
    - `src/screens/HudOverlay.test.ts` contains `vk-hud-build-attack`
    - `src/screens/HudOverlay.test.ts` contains `setBuildSelection('attack')`
    - `src/screens/HudOverlay.test.ts` contains `setActionAvailability({ attack: false, buff: true })`
    - `src/screens/HudOverlay.test.ts` contains `Action unavailable - not enough resources or invalid placement.`
    - `npx vitest run src/screens/HudOverlay.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "E:/py_scrypt/карта валкирикс" && npx vitest run src/screens/HudOverlay.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>HudOverlay test file exists, passes, and locks the command-bar contract for downstream wiring.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npx vitest run src/screens/HudOverlay.test.ts` exits 0
- `npx tsc --noEmit` exits 0
- HudOverlay owns a stable bottom command-surface API for the next integration plan
</verification>

<success_criteria>
1. The passive HUD is replaced by a structured bottom command bar
2. Real tower controls have explicit slots in the HUD contract
3. Staged unit slots are visible without pretending full gameplay support already exists
4. Result overlay language matches the command-layer family
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-in-game-menu-and-overlay-ux/02.2-01-SUMMARY.md`
</output>
