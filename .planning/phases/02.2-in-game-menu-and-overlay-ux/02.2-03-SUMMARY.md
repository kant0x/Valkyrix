---
phase: 02.2-in-game-menu-and-overlay-ux
plan: 03
status: complete
updated: 2026-03-21
---

# 02.2-03 Summary

## What Changed

- Finalized `src/screens/EscMenuOverlay.ts` as a compact in-match system overlay with explicit `Return to Battle`, music toggle, `Exit to Menu`, and destructive exit confirmation flow.
- Added focused regression coverage in `src/screens/EscMenuOverlay.test.ts` so ESC open/close, exit confirmation, and screen navigation stay stable.
- Polished `src/screens/HudOverlay.ts` after human review: the panel was reorganized around the citadel, command sections, and the Oracle briefing, with redundant HUD elements removed and the lower command surface accepted as the active in-match panel.
- Confirmed the panel now exists as the accepted Phase 02.2 command surface and the in-match UX family is coherent enough to close the phase.

## Verification

- `npx tsc --noEmit`
- `npx vitest run src/screens/HudOverlay.test.ts`
- Human verification approved on 2026-03-21 after reviewing the panel and Oracle cleanup in the running UI flow

## Notes

- The Oracle block was intentionally simplified during sign-off to avoid duplicate information and reduce visual noise.
- Phase `02.2` is now considered complete and no longer blocked on the earlier human-verification checkpoint.
