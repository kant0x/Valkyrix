---
phase: 04-boss-negotiation-mechanic
plan: "03"
type: tdd
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/screens/NegotiationOverlay.ts
  - src/screens/NegotiationOverlay.test.ts
autonomous: true
requirements:
  - BOSS-02

must_haves:
  truths:
    - "NegotiationOverlay mounts a DOM element with id 'vk-negotiation-overlay' into document.body"
    - "NegotiationOverlay renders exactly two choice buttons: 'Offer tribute' and 'Defy it'"
    - "Clicking 'Offer tribute' calls onSuccess callback and unmounts the overlay"
    - "Clicking 'Defy it' calls onFailure callback and unmounts the overlay"
    - "NegotiationOverlay.unmount() removes the DOM element"
    - "Calling mount() twice does not create duplicate overlays"
  artifacts:
    - path: "src/screens/NegotiationOverlay.ts"
      provides: "NegotiationOverlay class with mount(container, callbacks) and unmount()"
      exports: ["NegotiationOverlay", "NegotiationCallbacks"]
    - path: "src/screens/NegotiationOverlay.test.ts"
      provides: "jsdom unit tests for BOSS-02"
      contains: "describe('NegotiationOverlay'"
  key_links:
    - from: "src/screens/NegotiationOverlay.ts"
      to: "document.body"
      via: "mount() appends overlay element to document.body (not container)"
      pattern: "document.body.appendChild"
    - from: "src/game/BossSystem.ts"
      to: "src/screens/NegotiationOverlay.ts"
      via: "BossSystem imports and instantiates NegotiationOverlay"
      pattern: "from '../screens/NegotiationOverlay'"
---

<objective>
Implement and test the NegotiationOverlay — the full-viewport DOM modal that presents the two negotiation choices to the player.

Purpose: The overlay is a self-contained DOM component following the HudOverlay.showWinLossOverlay idiom. It is unit-testable in jsdom without BossSystem. BossSystem (Plan 02) will import and instantiate it.
Output: `NegotiationOverlay.ts` and `NegotiationOverlay.test.ts` with BOSS-02 behaviors fully covered.
</objective>

<execution_context>
@E:/py_scrypt/карта валкирикс/.claude/get-shit-done/workflows/execute-plan.md
@E:/py_scrypt/карта валкирикс/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-boss-negotiation-mechanic/04-RESEARCH.md
@.planning/phases/04-boss-negotiation-mechanic/04-01-SUMMARY.md

<interfaces>
<!-- DOM pattern from HudOverlay.ts showWinLossOverlay (the idiom to follow): -->

```typescript
// Pattern: create element, append to document.body, add click listeners
const OVERLAY_ID = 'vk-win-loss-overlay'; // NegotiationOverlay uses 'vk-negotiation-overlay'

showWinLossOverlay(result: 'won' | 'lost'): void {
  if (document.getElementById(OVERLAY_ID)) return; // idempotent guard
  const overlay = document.createElement('div');
  overlay.id = OVERLAY_ID;
  overlay.innerHTML = `...`;
  document.body.appendChild(overlay); // ALWAYS body, never container param
  overlay.querySelector('#vk-play-again')?.addEventListener('click', () => ...);
}
```

<!-- vitest.config.ts environment: 'jsdom' — DOM APIs available in tests -->
<!-- Test pattern used across codebase: jsdom, document.getElementById, click() -->

<!-- NegotiationCallbacks type (Plan 01 creates types, this plan creates the overlay) -->
```typescript
export type NegotiationCallbacks = {
  onSuccess: () => void;
  onFailure: () => void;
};
```
</interfaces>
</context>

<feature>
  <name>NegotiationOverlay — boss negotiation modal</name>
  <files>src/screens/NegotiationOverlay.ts, src/screens/NegotiationOverlay.test.ts</files>
  <behavior>
Mount (BOSS-02):
- mount(container: HTMLElement, cbs: NegotiationCallbacks): void
- If document.getElementById('vk-negotiation-overlay') exists → return early (idempotent)
- Create div with id='vk-negotiation-overlay', append to document.body (NOT container)
- Render: boss name "Devourer of Worlds", flavor text, two buttons: id='vk-neg-offer' ("Offer tribute") and id='vk-neg-defy' ("Defy it")
- Attach click listeners: offer → unmount() then cbs.onSuccess(), defy → unmount() then cbs.onFailure()

Unmount:
- unmount(): void
- Remove this.el from DOM; also remove by id as safety fallback
- Set this.el = null

Styling:
- Overlay is position:fixed, covers full viewport (inset:0), high z-index (120)
- Background: dark semi-transparent backdrop (matching HudOverlay win/loss overlay aesthetic)
- Panel: centered, dark background with amber/gold border tone, matching existing vk- design language
- Include inline `<style>` injected into document.head (same as HudOverlay pattern with STYLE_ID guard)
- Style ID: 'vk-neg-style'
  </behavior>
  <implementation>
Create `src/screens/NegotiationOverlay.ts`:

```typescript
const OVERLAY_ID = 'vk-negotiation-overlay';
const STYLE_ID = 'vk-neg-style';

export type NegotiationCallbacks = {
  onSuccess: () => void;
  onFailure: () => void;
};

export class NegotiationOverlay {
  private el: HTMLElement | null = null;

  mount(container: HTMLElement, cbs: NegotiationCallbacks): void {
    if (document.getElementById(OVERLAY_ID)) return;
    this.ensureStyle();
    this.el = document.createElement('div');
    this.el.id = OVERLAY_ID;
    this.el.innerHTML = `
      <div class="vk-neg-panel">
        <div class="vk-neg-rune">Parley</div>
        <div class="vk-neg-title">Devourer of Worlds</div>
        <p class="vk-neg-body">A dark intellect stands before the Citadel. Its voice resonates through the data lattice:</p>
        <p class="vk-neg-speech">"Stand aside and I shall spare your archives. Resist, and I shall consume them."</p>
        <div class="vk-neg-choices">
          <button id="vk-neg-offer" class="vk-neg-btn vk-neg-btn--offer" type="button">Offer tribute</button>
          <button id="vk-neg-defy" class="vk-neg-btn vk-neg-btn--defy" type="button">Defy it</button>
        </div>
      </div>
    `;
    document.body.appendChild(this.el);
    this.el.querySelector('#vk-neg-offer')?.addEventListener('click', () => {
      this.unmount();
      cbs.onSuccess();
    });
    this.el.querySelector('#vk-neg-defy')?.addEventListener('click', () => {
      this.unmount();
      cbs.onFailure();
    });
  }

  unmount(): void {
    this.el?.remove();
    this.el = null;
    document.getElementById(OVERLAY_ID)?.remove();
  }

  private ensureStyle(): void {
    if (document.getElementById(STYLE_ID)) return;
    const style = document.createElement('style');
    style.id = STYLE_ID;
    style.textContent = `
      #${OVERLAY_ID}{position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(2,4,10,.90);backdrop-filter:blur(14px);z-index:120}
      .vk-neg-panel{width:min(92vw,480px);padding:36px 32px;display:flex;flex-direction:column;align-items:center;gap:16px;text-align:center;border:1px solid rgba(168,123,76,.32);background:linear-gradient(180deg,rgba(22,22,24,.98),rgba(6,6,8,.99));box-shadow:0 48px 120px rgba(0,0,0,.6),inset 0 1px 0 rgba(255,226,182,.06)}
      .vk-neg-rune{font-family:"Copperplate Gothic Bold","Bahnschrift",sans-serif;font-size:11px;letter-spacing:.28em;text-transform:uppercase;color:#d1a06a}
      .vk-neg-title{font-family:"Copperplate Gothic Bold","Bahnschrift",sans-serif;font-size:28px;letter-spacing:.12em;text-transform:uppercase;color:#f0c17b}
      .vk-neg-body,.vk-neg-speech{margin:0;font-size:13px;line-height:1.6;color:#9cb7cf}
      .vk-neg-speech{color:#c8d8e8;font-style:italic}
      .vk-neg-choices{display:flex;gap:12px;margin-top:8px;width:100%}
      .vk-neg-btn{flex:1;padding:14px;border:1px solid rgba(170,126,77,.28);background:#0e1118;color:#ecf7ff;cursor:pointer;font-family:"Bahnschrift","Trebuchet MS",sans-serif;font-size:12px;letter-spacing:.18em;text-transform:uppercase;transition:border-color .15s,background .15s}
      .vk-neg-btn--offer:hover{border-color:rgba(120,220,160,.52);background:#081410}
      .vk-neg-btn--defy:hover{border-color:rgba(255,110,110,.4);background:#120808}
    `;
    document.head.appendChild(style);
  }
}
```

Tests (jsdom, vitest): verify mount creates element, buttons exist, callbacks fire, unmount cleans up, double-mount is idempotent.
  </implementation>
</feature>

<verification>
<automated>npx vitest run src/screens/NegotiationOverlay.test.ts --reporter=verbose</automated>
Full suite after: `npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- NegotiationOverlay.test.ts covers: mount creates DOM element, two buttons present, offer button calls onSuccess, defy button calls onFailure, unmount removes element, double-mount is idempotent
- All tests green
- NegotiationOverlay.ts compiles, exports NegotiationCallbacks and NegotiationOverlay
- `npx vitest run --reporter=verbose` full suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-boss-negotiation-mechanic/04-03-SUMMARY.md`
</output>
