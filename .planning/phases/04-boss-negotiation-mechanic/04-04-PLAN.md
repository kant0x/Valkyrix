---
phase: 04-boss-negotiation-mechanic
plan: "04"
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified: []
autonomous: false
requirements: [BOSS-01, BOSS-02, BOSS-03, BOSS-04]

must_haves:
  truths:
    - "Full test suite passes (npx vitest run exits 0)"
    - "Boss appears on field after ~5 minutes of gameplay"
    - "Negotiation overlay opens with scale bar and attempts counter"
    - "Successful negotiation returns game to playing phase"
    - "Failed negotiation triggers heavy+ranged horde and boss stays enraged"
  artifacts:
    - path: "src/game/BossSystem.ts"
      provides: "Timer trigger, boss spawn, success/failure handlers — verified working"
    - path: "src/screens/NegotiationOverlay.ts"
      provides: "Scale bar, 3-outcome Gemini dialog — verified working"
  key_links:
    - from: "src/main.ts"
      to: "src/game/BossSystem.ts"
      via: "bossSystem.update(dt, gameState, document.body) inside phase === 'playing' guard"
      pattern: "bossSystem\\.update"
---

<objective>
Run the full test suite to confirm BossSystem + NegotiationOverlay rewrites are integrated correctly, then perform human verification of the complete boss negotiation flow in the browser.

Purpose: Plans 04-02 and 04-03 rewrite the core systems in isolation. This plan verifies they work together end-to-end and that no regressions were introduced.
Output: Confirmed working Phase 4 boss negotiation mechanic.
</objective>

<execution_context>
@E:/py_scrypt/карта валкирикс/.claude/get-shit-done/workflows/execute-plan.md
@E:/py_scrypt/карта валкирикс/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-boss-negotiation-mechanic/04-CONTEXT.md
@.planning/phases/04-boss-negotiation-mechanic/04-02-SUMMARY.md
@.planning/phases/04-boss-negotiation-mechanic/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm all tests pass</name>
  <files></files>
  <action>
Run the complete Vitest test suite:
```
npx vitest run
```

All tests must pass, including:
- src/game/BossSystem.test.ts (new timer/spawn/horde tests)
- src/screens/NegotiationOverlay.test.ts (new scale/attempts/3-outcome tests)
- All Phase 3 test files (no regressions)

Run TypeScript compiler:
```
npx tsc --noEmit
```

Must exit 0 with no errors.

If any test fails: diagnose the failure, fix the source file (BossSystem.ts or NegotiationOverlay.ts), re-run until green. Do NOT skip or modify tests to make them pass — fix the implementation.

Common failure points to check:
- BossSystem.test.ts: NegotiationOverlay mock not correctly set up → verify vi.mock path matches actual import
- NegotiationOverlay.test.ts: pending flag not reset after mocked fetch → check error path in sendMessage
- TypeScript: NegotiationMountOptions not exported from NegotiationOverlay.ts → add export
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -30 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    `npx vitest run` exits 0 with all tests passing.
    `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of boss negotiation flow in browser</name>
  <action>Human verifies the complete boss negotiation mechanic end-to-end in the browser after automated tests pass.</action>
  <what-built>
    Complete Phase 4 boss negotiation mechanic:
    - BossSystem: timer-based trigger at 300s (5 min), boss unit spawned, scale/attempts state machine
    - NegotiationOverlay: full-screen dark overlay, scale bar 0→12, attempts counter, free-text input, Gemini AI responses
    - Three outcomes: good (+4 scale), neutral (+2 scale, +2 attempts), bad (attempt burned)
    - Success (scale >= 12): boss leaves, resources credited, game resumes
    - Failure (attempts = 0): heavy+ranged horde spawns, boss stays enraged
    - All tests passing, TypeScript clean
  </what-built>
  <how-to-verify>
    Run the dev server: `npm run dev` (or `npx vite`)

    **Test 1 — Timer trigger (fast test using browser console):**
    1. Start a game session
    2. In browser DevTools console, run: `gameState.elapsed = 295` (or equivalent access path)
    3. Wait 5 seconds — boss should appear on field and negotiation overlay should open
    4. Confirm overlay shows: boss name/portrait area, boss opening line, scale bar at 0/12, "Попыток: 3"

    **Test 2 — Negotiation outcomes:**
    1. Type a message and submit
    2. Confirm Gemini responds with boss-character reply in Russian
    3. Confirm scale bar or attempts counter updates correctly based on outcome
    4. Try several rounds until scale >= 12 OR attempts = 0

    **Test 3 — Success path:**
    1. Convince boss (scale reaches 12)
    2. Confirm overlay closes after ~3 seconds
    3. Confirm game resumes (units moving again)
    4. Confirm resources increased

    **Test 4 — Failure path (may require new game session):**
    1. Exhaust all attempts with bad responses
    2. Confirm overlay closes
    3. Confirm heavy + ranged enemy horde spawns on field
    4. Confirm boss remains on field (enraged)

    Type "approved" if all paths work correctly, or describe any issues found.
  </how-to-verify>
  <verify>Human confirms all 4 test scenarios work correctly.</verify>
  <done>Human types "approved" or all issues are resolved.</done>
  <resume-signal>Type "approved" to complete Phase 4, or describe issues for a fix cycle</resume-signal>
</task>

</tasks>

<verification>
Human confirmation that boss trigger, negotiation dialog, success path, and failure path all work in the browser.
</verification>

<success_criteria>
- Full test suite green (npx vitest run exits 0)
- TypeScript compiles clean
- Boss appears after 5 min elapsed
- Negotiation overlay functional with Gemini AI
- Success and failure outcomes both work correctly
- Human approves the mechanic
</success_criteria>

<output>
After completion, create `.planning/phases/04-boss-negotiation-mechanic/04-04-SUMMARY.md`
</output>
