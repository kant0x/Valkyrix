---
phase: 03-units-buildings-combat
plan: 04
type: tdd
wave: 3
depends_on: [03-02-PLAN.md, 03-03-PLAN.md]
files_modified:
  - src/game/CombatSystem.ts
  - src/game/CombatSystem.test.ts
autonomous: true
requirements: [GAME-01]

must_haves:
  truths:
    - "When an enemy unit and ally unit occupy the same path segment (within 32 world units), both stop and fight"
    - "Fighting units deal damage to each other every 1/attackRate seconds until one reaches hp <= 0"
    - "Dead unit is instantly removed (hp <= 0 filtered by UnitSystem); winner resumes 'moving' state"
    - "Enemy unit reaching player base end deals continuous damage until citadel-equivalent hp hits 0"
    - "When citadel hp reaches 0: state.phase = 'lost'"
    - "When all waves are exhausted and no enemies remain: state.phase = 'won'"
  artifacts:
    - path: "src/game/CombatSystem.ts"
      provides: "CombatSystem class — update(dt, state) handling unit-vs-unit collision, base attacks, win/loss conditions"
      exports: ["CombatSystem"]
  key_links:
    - from: "src/game/CombatSystem.ts"
      to: "src/game/ResourceSystem.ts"
      via: "registerKill(defKey, state) called when enemy unit dies"
      pattern: "registerKill"
    - from: "src/game/CombatSystem.ts"
      to: "src/game/game.types.ts"
      via: "Unit, GameState, UNIT_DEFS imported"
      pattern: "from.*game\\.types"
---

<objective>
Implement combat resolution: unit-vs-unit fighting on the path, base attacks, win/loss state transitions.

Purpose: After this plan the game has a win and a loss condition — citadel destruction or surviving all waves. This closes the core gameplay loop.
Output: CombatSystem with full unit-vs-unit damage logic, base attack phase, and phase transitions to 'won'/'lost'.
</objective>

<execution_context>
@C:/Users/Kant/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Kant/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-units-buildings-combat/03-CONTEXT.md
@.planning/phases/03-units-buildings-combat/03-RESEARCH.md
@.planning/phases/03-units-buildings-combat/03-02-SUMMARY.md
@.planning/phases/03-units-buildings-combat/03-03-SUMMARY.md

<interfaces>
<!-- From game.types.ts and prior plans -->

Unit combat states:
```typescript
unit.state: 'moving' | 'fighting' | 'attacking-base'
unit.fightingWith: number | null  // id of opponent
unit.attackCooldown: number       // seconds until next attack
unit.hp: number                   // when <= 0, filtered by UnitSystem
```

Collision radius: 32 world units (half of tileWidth=64). Per RESEARCH.md Pitfall 2 — express in tile-widths not pixels.

Base attack logic (from CONTEXT.md decisions):
- Enemy reaches end of path → state='attacking-base' → attacks citadel (state.citadelHp) repeatedly
- Ally reaches reversed-path end → state='attacking-base' → attacks player base (state.playerBaseHp) — NOTE: there is no "player base" in the traditional sense here; our ally units protect the citadel. Revisit: ally units attacking-base means they've reached the enemy portal end and deal damage to playerBaseHp. This is a future multiplayer mechanic; for Phase 3 solo mode, ally units' 'attacking-base' at portal end simply stop (no playerBaseHp damage — Phase 5 concern). CombatSystem should leave ally 'attacking-base' units idle for now.

Win condition (CONTEXT.md):
- Proigrysh (loss): citadelHp = 0
- Pobeda (win): state.waveNumber >= MAX_WAVES and state.spawnQueue.length === 0 and state.units.filter(u=>u.faction==='enemy').length === 0
- MAX_WAVES = 5 (hardcode in WaveController — after wave 5 no more spawns; won when last enemy dies)

From ResourceSystem.ts (Plan 03):
```typescript
export function registerKill(defKey: string, state: GameState): void
// Adds +5/+15/+10 electrolatov based on defKey
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: CombatSystem — unit-vs-unit, base attacks, win/loss transitions</name>
  <files>src/game/CombatSystem.ts, src/game/CombatSystem.test.ts</files>
  <behavior>
    Collision detection:
    - Enemy and ally unit within 32 world units on path: both set state='fighting', fightingWith=each other's id
    - Units already fighting: check if opponent still alive; if not, set winner state='moving', fightingWith=null

    Damage tick:
    - Fighting unit with attackCooldown <= 0: deal unit.def.damage to opponent's hp; reset attackCooldown = 1/unit.def.attackRate
    - Decrement all fighting units' attackCooldown by dt each frame
    - When enemy hp <= 0: call registerKill(unit.def.role + '-enemy', state) — note: CombatSystem sets hp=0; UnitSystem filter removes it next frame

    'attacking-base' for enemies:
    - Enemy in 'attacking-base': deals unit.def.damage every 1/unit.def.attackRate seconds to state.citadelHp
    - state.citadelHp = max(0, state.citadelHp - damage)

    Win/loss checks (at end of each update):
    - If state.citadelHp <= 0: state.phase = 'lost'
    - If state.waveNumber >= 5 and state.spawnQueue.length === 0 and no enemy units remain: state.phase = 'won'
    - If state.phase !== 'playing': early return (no further updates)

    Ally 'attacking-base': set idle (no damage) — Phase 5 concern.
  </behavior>
  <action>
    RED: Write CombatSystem.test.ts with 8 tests (cases above). Synthetic state objects — no file I/O.
    Test cases must include: collision triggers fighting state, damage dealt after cooldown, kill awards resources, citadel damage from 'attacking-base' enemy, loss triggered at 0 hp, win triggered at wave 5 + no enemies.

    GREEN: Create src/game/CombatSystem.ts:
    ```typescript
    import { registerKill } from './ResourceSystem';
    import type { GameState, Unit } from './game.types';

    export class CombatSystem {
      update(dt: number, state: GameState): void {
        if (state.phase !== 'playing') return;
        this.resolveCollisions(state);
        this.tickFighting(dt, state);
        this.tickBaseAttacks(dt, state);
        this.checkWinLoss(state);
      }
      // private methods...
    }
    ```

    Collision resolution: O(n²) scan over enemy/ally pairs is fine — unit counts are small (max ~30 per wave × 2 factions = ~60 units). Use Math.hypot(dx,dy) < 32.

    Note: do NOT delete units inside CombatSystem — only set hp=0. UnitSystem.update() already filters hp<=0 at end of its frame. Call order in main.ts will be: WaveController → UnitSystem → BuildingSystem → ProjectileSystem → CombatSystem (CombatSystem last so projectile damage is visible to win/loss check in same frame).

    Run until all tests pass.
  </action>
  <verify>
    <automated>cd "E:/py_scrypt/карта валкирикс" && npx vitest run src/game/CombatSystem.test.ts</automated>
  </verify>
  <done>All CombatSystem tests pass; unit-vs-unit combat works; citadel HP drains; win/loss transitions set state.phase correctly; tsc clean</done>
</task>

</tasks>

<verification>
After completion:
- `npx vitest run src/game/CombatSystem.test.ts` — all green
- `npx tsc --noEmit` exits 0
- CombatSystem does NOT mutate state.units array directly (no splice) — sets hp=0 only
- registerKill called with correct defKey when enemy hp hits 0
- state.phase transitions only happen in CombatSystem.checkWinLoss()
</verification>

<success_criteria>
1. Enemy and ally units within 32 world units stop and fight to the death
2. Winner resumes moving; loser is marked dead (hp=0) and removed next frame by UnitSystem
3. Enemies reaching citadel end deal continuous damage until citadel falls (state.phase='lost')
4. After wave 5 with no enemies remaining: state.phase='won'
5. All combat logic stops when phase is not 'playing'
</success_criteria>

<output>
After completion, create `.planning/phases/03-units-buildings-combat/03-04-SUMMARY.md`
</output>
