# Phase 3: Units, Buildings & Combat - Context

**Gathered:** 2026-03-17
**Status:** Ready for planning

<domain>
## Phase Boundary

Базовый игровой цикл: враги движутся по path к цитадели, игрок строит башни в zones и отправляет своих юнитов навстречу. Волны врагов нарастают по сложности. Цитадель умерла — проигрыш, пережил все волны — победа. Мультиплеер, блокчейн, переговоры с боссом — следующие фазы.

</domain>

<decisions>
## Implementation Decisions

### Движение юнитов по path
- Один общий path в map JSON; враги идут справа налево, наши юниты слева направо по тем же узлам
- Движение плавное (lerp между узлами), направление определяет ряд анимации из спрайт-атласа
- Несколько юнитов одновременно на пути — у каждого свой прогресс по path
- Дошёл до конца своего маршрута → атакует базу/цитадель противника постоянно до уничтожения

### Встреча на пути
- Наш юнит встречает врага на path → оба останавливаются, дерутся, победитель идёт дальше
- Смерть юнита: мгновенное исчезновение (без анимации смерти)

### Волны врагов
- Запуск автоматический по таймеру (каждые 10–15 сек)
- 15–30 юнитов на волну, спавн из портала с фиксированной задержкой между юнитами (~0.5с)
- Первые волны: большие орды слабых юнитов
- Усложнение от волны к волне: больше юнитов + появляются новые типы
- Проигрыш: HP цитадели = 0

### Здания
- Два типа: **атакующая башня** (стреляет по врагам в радиусе) и **башня-баф** (усиливает наших юнитов поблизости)
- Размещение: выбрать из меню → кликнуть на допустимую zone на карте
- Можно снести башню во время игры (возврат части ресурсов — на усмотрение Claude)
- Атакующая башня: цель — ближайший враг в радиусе, визуально — проектиль летит к цели (assets в `public/assets/projectiles`)

### Ресурсы (электролаты)
- Два источника: башни производят пассивно + убитые враги дропают
- Электролаты тратятся на постройку башен

### Claude's Discretion
- Точные числа (HP юнитов, урон башен, радиус атаки, стоимость башен) — Claude балансирует
- Механика возврата ресурсов при сносе башни
- Конкретный баф башни-бафа (скорость, урон, или регенерация)
- Формат хранения состояния юнитов в runtime

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/screens/HudOverlay.ts`: готовый HUD из фазы 2 — расширить для отображения HP цитадели, волн, электролатов
- `src/screens/ScreenManager.ts`: маршрутизация экранов — добавить GameScreen через него
- `src/shared/CameraMath.ts`: изометрические координаты уже решены — юниты используют те же формулы
- `public/assets/projectiles/`: спрайты снарядов уже есть — использовать для атак башен
- `public/assets/pers/`: спрайт-атласы юнитов (направления движения, анимации)

### Established Patterns
- `requestAnimationFrame` loop в `src/main.ts` — весь game loop идёт через него; юниты, башни, волны обновляются в `update()` и рендерятся в `render()`
- Map data из `public/assets/maps/active-map.json` — `paths` и `zones` слои уже есть в JSON-контракте
- Все сущности рендерятся на `<canvas id="vk-canvas">` — юниты и башни тоже

### Integration Points
- `src/main.ts` `RuntimeState`: добавить `units[]`, `buildings[]`, `waves`, `resources` в объект состояния
- `src/screens/HudOverlay.ts`: подключить отображение игровых метрик (HP базы, номер волны, электролаты)
- `public/assets/maps/active-map.json`: читать `paths` узлы для маршрутов, `zones` тайлы для допустимых мест постройки

</code_context>

<specifics>
## Specific Ideas

- Атлас юнитов уже разбит по движениям (каждое направление — отдельный ряд/колонка)
- Враги — большие орды слабых юнитов в начале, потом появляются тяжёлые/новые типы
- Электролаты — внутриигровая валюта (не блокчейн, это фаза 5)

</specifics>

<deferred>
## Deferred Ideas

- Мультиплеер (у каждого игрока своя сторона) — Phase 5
- Блокчейн-транзакция за каждого убитого юнита — Phase 5
- Переговоры с боссом — Phase 4
- Апгрейд башен — можно добавить в backlog

</deferred>

---

*Phase: 03-units-buildings-combat*
*Context gathered: 2026-03-17*
